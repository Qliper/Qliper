import { FastifyInstance } from 'fastify'

export default async function (app: FastifyInstance) {
  // app.get('/', CampaignsController.index)
  // app.post('/', CampaignsController.store)
  // app.get('/:id', CampaignsController.show)
  // app.put('/:id', CampaignsController.update)
  // app.delete('/:id', CampaignsController.destroy)
}
