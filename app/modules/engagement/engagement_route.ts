import { FastifyInstance } from 'fastify'

export default async function (app: FastifyInstance) {
  // app.post('/verify', EngagementController.verify)
  // app.get('/stats', EngagementController.stats)
  // app.post('/claim-rewards', EngagementController.claimRewards)
}
