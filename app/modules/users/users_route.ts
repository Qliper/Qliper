import { FastifyInstance } from 'fastify'

export default async function (app: FastifyInstance) {
  // app.get('/profile', UsersController.profile)
  // app.put('/profile', UsersController.updateProfile)
  // app.get('/earnings', UsersController.earnings)
  // app.post('/connect-social', UsersController.connectSocial)
}
